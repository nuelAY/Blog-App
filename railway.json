{
  "build": {
    "cmd": "cd frontend && npm install && npm run build && npm run export && cd ../backend && npm install && npm run build"
  },
  "start": {
    "cmd": "cd backend && npm run start"
  }
}
